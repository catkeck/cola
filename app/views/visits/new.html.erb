<%= form_for(:visit, url: visits_path) do |f| %>
  <%= f.label :code %>
  <%= text_field_tag :code %></br>
  <%= f.label :store %>
  <%= f.select :store_id, @stores.collect { |store| [store.name, store.id]} %></br>
  <%= f.submit %>
<% end %>